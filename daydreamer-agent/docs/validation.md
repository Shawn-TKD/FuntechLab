# 第一版验证记录

## 2026-09-23：有上限的自动超时重提

- 默认每段提交成功后10分钟仍PENDING/RUNNING时，再查一次；仍未完成则沿用同一提示词和前段末帧自动重提，最多1次。旧任务记录保留，不执行远端取消。
- 提交时间、替代关系与自动重提次数落盘；恢复任务不重置计时与次数。没有提交时间的历史任务从首次恢复观察开始计时。
- 新尝试仍超时进入needs_resolution；后续可查询该任务的迟到结果，但不再自动突破次数上限。提交未知、查询错误、明确失败与下载错误不触发此规则。
- CLI render默认等待1800秒，参数范围仍为0–3600；命令退出后需恢复，未建立后台定时任务。
- 75项本地测试通过，新增覆盖自动等待计时、最终复查成功、同提示词与末帧复用、下游绑定新版本、恢复后次数限制、提交不明与查询异常、旧任务时间缺失、禁用策略及参数校验。本次未提交真实付费生成任务。

## 2026-09-23：Wan复用骑行故事的真实生成

- 任务`20260922T173429-ef4246a2da`，直接复用`20260922T170931-a3e9ddab94`的《石墩琴键》完整故事，故事校验值一致，本轮未调用Qwen。
- 三段全部使用wan3.0-video-prime，首段文生视频，后两段以实际剪辑末帧续接。每段4秒、1280×720、30fps，无原生音轨。
- 第二段首次提交持续RUNNING；用户明确要求重新提交后保留旧远端任务记录，创建第2次尝试。第2次尝试成功，第三段从该版本续接。最终选用尝试编号为1、2、1，旧第二段未参与拼接，未执行远端取消。
- 已导出12秒无声预览至`outputs/daydreamer-cycling02-wan-preview-20260923/连续续接_无声预览.mp4`，同目录保留分镜与运行记录。
- 本次通过文件技术检查与片段依赖检查；未做媒体内容检查，尚未添加音乐与音效。

## 2026-09-23：切换至wan3.0-video-prime

- 首段与续接段均使用wan3.0-video-prime；适配器改为providers/wan.py，不再提供旧模型提交入口。历史任务、视频与以下历史记录保持原样。
- 按官方接口设置首帧ratio=adaptive、audio=false、prompt_extend=false；校验图片尺寸和透明通道，提示词上限改为20000字符。当前应用单镜制作策略仍为3–15秒。
- 63项本地测试通过，覆盖异步调用、两段使用同一模型、真实末帧传递、恢复与重做、禁用音轨及自动改写、旧模型拒绝提交、透明首帧拒绝等。
- 使用已保存的真实末帧验证了新请求组装，未发送网络请求。本次未进行Wan真实付费生成，不能据此宣称账号已有该模型权限或确认其生成质量。

以下记录为切换前历史，不代表旧视频由Wan生成。

## 2026-09-23：骑自行车02事件卡试跑

- 输入：`outputs/daydreamer-prd/生活事件卡_骑自行车02.json`。原卡与任务快照一致，只读取文字，没有读取卡片引用的图像或源音轨。
- 任务：`20260922T170931-a3e9ddab94`；主题《石墩琴键》，Qwen生成三段各4秒的故事与分镜。
- 修正了两个格式兼容问题：关键帧引用不强制重复本地图片路径；与continuity完全一致的结构化起止状态无损转为文字，提示词不重复发送同一份状态。冲突或伪造的引用与状态仍会被拒绝。60项自动化测试通过。
- 本轮保留原始模型响应及格式转换结果，复用分镜检查点完成Qwen文本复核；没有人工改写情节。
- 三个视频任务均首次生成成功：首段t2v，后两段i2v，逐段传入前段实际剪辑末帧。
- 预览技术规格：12秒、1280×720、30fps、无音轨。输出：`outputs/daydreamer-cycling02-preview-20260923/连续续接_无声预览.mp4`，同目录保存故事与分镜及运行记录。
- 没有媒体内容检查，不能将技术检查等同于完全连贯的画面验收。音乐与音效仍待接入，任务状态为awaiting_audio。

## 2026-09-23：首帧续接实现

- 新任务默认 frame_chain；首段 t2v，后段 i2v。每段以最终剪辑使用的前段末帧作为首帧输入。
- 56 项自动化测试通过，包含图生视频参数、图片传递、串行恢复、重做后段失效与旧尝试保留、拒绝混用不同前段版本、明确的前镜状态引用展开、畸形状态引用报错，以及事件卡音频和关键帧编号的引用校验。
- FFmpeg 集成测试使用运动图样验证：源视频裁切与30fps归一化后，导出的末帧与最终拼接边界解码像素完全一致；最终拼接不保留源音轨。
- 自动化测试不判断真实生成画面在接缝处是否自然连贯，媒体内容检查保持关闭。

### 真实续接样片

- 输入仍为用户提供的小巷生活事件卡，原文件未修改。使用12秒、16:9、720P参数。
- 任务 `20260922T164851-fac651330b`，主题《门灯折巷》，三段各4秒。
- 第一段 `happyhorse-1.1-t2v`，第二、三段 `happyhorse-1.1-i2v`，三个远端任务均首次生成成功。
- 每个后段均使用其前段 `prepared.mp4` 的末帧，任务档案保存实际文件哈希、尝试版本及任务编号。已通过片段依赖、解码、时长、尺寸与帧率检查。
- 已导出12秒、1280×720、30fps无声预览：`outputs/daydreamer-continuity-preview-20260923/连续续接_无声预览.mp4`，并保存可读分镜、完整故事JSON与运行记录。
- Qwen 首次候选的声音编号引用曾被错误拒绝，已修正校验器；后续两次修订仍未逐字复用交接字段。本次助手对到达后的状态字段及右侧店门位置做了文字校正，保存 `story/editorial-changes.json` 与全部原始响应，故事来源标记 `qwen_with_recorded_editorial_corrections`。
- 后续创作已支持显式引用前镜结束状态后由程序展开；此机制通过离线测试，本条样片采用上述记录在案的文字校正结果。
- 没有检查媒体内容或认定接缝完全自然；音乐和音效未接入，状态保留 `awaiting_audio`。

## 2026-09-23：指定事件卡的完整视频试跑

- 输入：`outputs/daydreamer-prd/生活事件卡_示例.json`，完整事件卡格式，原始文件未修改。本轮仅使用卡片文字，没有读取其引用的原始媒体。
- 任务：`20260922T161849-987e770080`（任务编号使用 UTC）。
- Qwen 生成主题《风铃校准小巷》，三镜时长依次为 4、5、3 秒。
- HappyHorse 三个真实视频任务均首次生成成功，已下载并通过文件技术检查。
- 已拼接 12 秒、1280×720、30 fps 无声预览，输出至 `outputs/daydreamer-preview-20260923/风铃校准小巷_无声预览.mp4`。
- 同目录保存可读故事分镜、完整 JSON 和本轮运行记录；项目内保存原始镜头与全部检查点。
- 没有做媒体内容检查；没有加入音乐和音效，任务保留为 awaiting_audio。
- 补齐完整事件卡输入适配后，39 项测试通过。

以下为此前第一版开发阶段记录；其中“视频尚未实测”的限制已由上述试跑更新。

日期：2026-09-22。所有模型输入均为 examples/events.json 中明确标注的虚构测试卡，未上传用户视频或真实生活素材。

## 2026-09-23 原视频到短片的一键流程

- 默认 `daydreamer.cmd` 选择原视频后自动提取事件卡，衔接故事、分镜、Wan视频生成和合成导出。JSON入口和 `-Extract` 独立提取保留。
- 113项自动化测试通过。新增10项全流程测试覆盖视频理解到最终导出、事件卡随成片保存、统一编号恢复、理解失败/故事失败/远端视频运行中的恢复、制作参数保留、来源关联防混用和无事件时不启动后续模型。
- 全流程测试使用离线模型替身，不包含新增付费调用。验证了恢复不会再次调用已完成的理解/故事阶段、不会重复提交已完成的片段。启动器的默认帮助入口和 `-Extract` 帮助入口通过检查。

## 2026-09-23 视频提取生活事件卡

- 新增 `daydreamer.cmd -Extract`、视频路径参数及 `-Extract -Run` 恢复。已验证启动器参数与帮助入口，未在自动测试中打开文件选择弹窗。
- 103项自动化测试通过，新增19项事件卡提取测试。包含严格结构、ID/时间/交叉引用、禁填图片和声音、分段时间偏移、合并证据范围、格式修正上限、响应复用、正常卡片导出及既有故事输入兼容。
- 真实FFmpeg测试验证：从前半段红色、后半段蓝色的有声视频中截取后半段，输出为蓝色、时长正确、无音轨的理解副本。整个提取流程不保存图片。
- 使用6秒人工几何动画进行一次真实百炼调用，任务 `20260923T022434-61a5a46392`。`qwen3.8-max` 接受视频Data URL与严格JSON Schema，准确描述红色矩形在灰色条带移动，并返回no_event；程序没有导出空事件卡。接口报告3707 tokens，其中视频输入1368 tokens。
- 此次真实调用验证接口与无事件分支，不代表真实生活视频的识别质量已经验证。正常事件卡与恢复路径由离线测试覆盖；未调用Wan视频生成。

## 2026-09-23 一键运行验证

- 新增 `daydreamer.cmd`：使用项目虚拟环境，支持弹窗选择事件卡、直接传入 JSON 路径和 `-Run` 恢复。已在项目外目录验证启动器帮助入口；未在自动测试中打开交互式选取窗口。
- `run` 自动串联故事创作、逐段续接、5分钟超时重提、拼接与交付导出。参数缺省为12秒、16:9、720P；事件卡参数及显式参数优先。
- 84项测试全部通过，包含9项新增工作流测试。离线模拟覆盖五阶段故事创作、两段视频续接、合成、导出、恢复复用、未完成时停止、参数优先级、密钥路径配置与导出完整性检查。
- 原有真实 FFmpeg 集成测试通过。本次功能验证没有新增付费模型请求；离线模拟不代表新模型画面效果。
- 默认仍为无声预览；配置本地音频清单可合成带音乐音效的成片。

以下为早期开发验证记录，模型及测试数量保留当时状态。

## 已验证

- Python 3.12.14 虚拟环境隔离有效，运行代码无需第三方 Python 包。
- FFmpeg/FFprobe 在项目内可用，下载包已核对供应方 SHA-256。
- 34 项自动化测试通过。包括事件引用与时长约束、四项提示词、并发写入锁、原任务恢复、已成功镜头复用、失败单镜重试、提交超时不重发、密钥不随视频下载发送。
- 媒体集成测试使用测试图样和纯音信号，验证成片尺寸及时长、剔除原片音轨、背景音乐循环铺满、音效按指定时间进入、无声预览。
- 项目源文件、文档、任务 JSON 和日志扫描未发现原始 API Key。

## Qwen 真实接口联调

模型：qwen3.8-max。流程：主题 → 脚本 → 视觉风格 → 完整故事和分镜 → 文本复核。

- 成功任务：`20260922T155331-9a13bf8d68`。
- 主题标题：铃波落叶。
- 输出：1 个 6 秒镜头，总时长 6 秒，16:9，480P。
- 故事结构、事件引用、镜头时长和视频请求参数校验通过。
- 本轮包含一次输出修订，共 6 次文本模型请求；平台返回的合计 token 使用量为 44,575。该数量仅描述本轮，不等于费用。
- 输出保存在 `data/runs/20260922T155331-9a13bf8d68/story/result.json`。

前一次开发联调发现模型输出三段 2 秒镜头，已将最短 3 秒限制从提示词建议改为程序强制校验，并触发模型修订；原任务保留为 needs_resolution，不用于提交视频。

## 本地合成演示

- 任务：`20260922T154841-64a6497292`。
- 结果：6 秒 MP4，包含两段测试图样及测试音调，非模型创作视频。
- 文件：`data/runs/20260922T154841-64a6497292/final/video.mp4`。
- 已保存所用片段、音轨、时间线、成片及 manifest。

## 尚未验证或尚未接入

- HappyHorse 已实现真实接口适配，但本轮没有提交付费视频任务；实际视频生成权限、输出质量和远端下载仍需首条样片联调。
- 当前仅支持本地音乐和音效，自动音频生成服务未确定。
- 按需求没有媒体内容检查；第一人称、主题一致等内容由创作文本约束，不宣称已在生成画面上验证。
- 世界记忆只读取与保存增量草案；不自动提交新世界规则。
- 当前为命令行版本，未开发图形界面；剪辑只采用顺序硬切。
